# Minimal Python runtime for ``python -m src.api_bridge`` in the Express image.
# Keep this boundary small; the FastAPI/kernel image uses requirements-runtime.txt.
prometheus-client>=0.14.0
psycopg2-binary>=2.9.0
pydantic>=2.4.0
python-dotenv>=1.2.2
pyyaml>=6.0.1,<7.0.0
requests>=2.33.0
