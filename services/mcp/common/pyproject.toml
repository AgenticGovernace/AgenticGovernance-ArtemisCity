[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "artemis-mcp-common"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mcp[cli]==2.0.0",
    "pydantic>=2.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
