# Grafana datasource provisioning for the Artemis City compose stack.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: artemis-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
