schema_version: 1
name: prod
description: Production environment reached only through the reviewed promotion cascade.
runtime:
  log_level: WARN
  debug: false
  reload: false
governance:
  strict_atp: true
  trust_default_level: 1
deploy:
  branch: prod
  github_environment: prod
