schema_version: 1
name: staging
description: Pre-production environment mirroring production policy with synthetic
  data.
runtime:
  log_level: INFO
  debug: false
  reload: false
governance:
  strict_atp: true
  trust_default_level: 1
deploy:
  branch: staging
  github_environment: staging
