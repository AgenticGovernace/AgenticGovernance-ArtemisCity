"""Artemis City application packages.

Top-level container for the ``app/*`` packages shipped in the wheel
(currently ``app.kernel``). Kept as an explicit package so Hatch can
include the tree and installed consumers can import ``app.kernel.*``.
"""
