# Artemis dashboard — generated from root .env by setup_secrets.sh.
# These browser-visible values are derived aliases of FASTAPI_API_KEY and
# MCP_API_KEY. Configure or rotate the source values only in root .env.
VITE_FASTAPI_API_KEY=
VITE_MCP_API_KEY=
