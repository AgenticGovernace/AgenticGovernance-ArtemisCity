OBSIDIAN_VAULT_PATH=path/to/your/obsidian/vault
# Make sure to replace 'path/to/your/obsidian/vault' with the actual path to your Obsidian vault on your system.
OPENAI_API_KEY=your_openai_api_key  
# Optional: Set the model you want to use (e.g., gpt-3.5-turbo, gpt-4)
OPENAI_MODEL=gpt-3.5-turbo
# Optional: Set the temperature for the model (0.0 to 1.0)
OPENAI_TEMPERATURE=0.7
# Optional: Set the maximum tokens for the response
OPENAI_MAX_TOKENS=1500
# Optional: Set the system prompt for the model
OPENAI_SYSTEM_PROMPT=You are an assistant that helps with Obsidian vault management and
note-taking. Provide concise and helpful responses based on the user's queries about their vault. You can also provide tips and best practices for organizing and maintaining the vault. Make sure to ask clarifying questions if the user's query is ambiguous or lacks details.
# Optional: Set the log level (e.g., DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
# Optional: Set the path for the log file
LOG_FILE=obsidian_ai_assistant.log
#.env.example is a template for the .env file that contains environment variables for the Obsidian AI Assistant. You should copy this file to .env and fill in the appropriate values for your setup. The variables include the path to your Obsidian vault, your OpenAI API key, and optional settings for the model, temperature, max tokens, system prompt, log level, and log file path.