name: Deploy

on:
  push:
    branches: [development, main, staging, prod]
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment
        required: true
        default: development
        type: choice
        options: [development, main, staging, prod]

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  resolve:
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.pick.outputs.environment }}
    steps:
      - id: pick
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "environment=${{ github.event.inputs.environment }}" >> "$GITHUB_OUTPUT"
          else
            branch="${GITHUB_REF##*/}"
            case "$branch" in
              dev|staging|prod) echo "environment=$branch" >> "$GITHUB_OUTPUT" ;;
              *) echo "Unsupported branch: $branch" >&2; exit 1 ;;
            esac
          fi

  deploy:
    needs: resolve
    runs-on: ubuntu-latest
    environment: ${{ needs.resolve.outputs.environment }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Load environment config
        env:
          ARTEMIS_ENV: ${{ needs.resolve.outputs.environment }}
        run: |
          pip install pyyaml
          python - <<'PY'
          import os, json
          from src.utils.environments import load_environment
          cfg = load_environment(os.environ["ARTEMIS_ENV"])
          print(json.dumps(cfg, indent=2))
          PY\